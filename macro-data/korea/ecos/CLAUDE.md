# Bank of Korea ECOS

- Use the registered `ECOS_API_KEY` alias and the provider definition in the shared store's `config\providers.json`.
- Follow the shared import and credential-handling flow in `{{CLAUDE_ROOT}}\MD\standards\DATA_ACCESS.md`.
- Request only the series, dimensions, and periods required for the task.
- Preserve the statistic or table code, item code, frequency, unit, adjustment status, observation period, and retrieval date.
